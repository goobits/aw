# Shared Agent Guidance

This submodule publishes the shared `.agents` bundle at `agents/.agents`.

Shelly owns interactive terminal sessions and the optional commit-owner queue.
The repository does not provide a separate agent-workspace executable.
